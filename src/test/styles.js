import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
    },
    text: {
        padding: 5,
        marginVertical: 15,
        backgroundColor: '#aaa',
        fontSize: 15,
        textAlign: 'center',
    },
});
